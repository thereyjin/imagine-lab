# 项目 AI 对话与画布验收

## 启动

```sh
npm run build
npm run preview:real
```

打开 http://127.0.0.1:4174/ 。开发时另开终端运行 `npm run dev`，Vite 会把 `/api` 转到 4174。

在首页 API 设置或聊天输入框原有的“技能”按钮内打开“模型设置”，填写 OpenAI API Key 与有权限的模型，点击“保存并测试连接”。也可通过服务进程环境变量 `OPENAI_API_KEY`、`OPENAI_MODEL` 配置。`OPENAI_BASE_URL` 仅供部署者指定兼容 Responses API 的服务地址，浏览器不能指定外部调用地址。

密钥只保存在本机服务内存（或部署者环境变量），不存入浏览器、不写到源码、不会返回给前端。重启服务后，通过 UI 输入的密钥需重新填写。测试连接会真实调用模型。

## 验收步骤

1. 打开已有导入项目，确认左侧沿用原来的标题栏、消息气泡、步骤胶囊、历史面板和输入框；没有额外的模型状态栏或欢迎卡片。
2. 输入“梳理当前项目的页面结构”。应出现工具执行状态与结果，内容对应当前项目。
3. 从画板原有“修改”菜单选择“用 AI 修改”，点击页面元素，在就地输入框填写意见并确认；可连续标注多处，最后统一提交到左侧对话。导入页使用真实节点 ID，不再使用居中弹窗替代批注。
4. AI 应先查看页面，再提供待应用方案；此时画布尚未改变。展开变更清单检查目标。
5. 点击“应用到画布”，文案实际更新；点击“撤回本次修改”，恢复旧内容。
6. 请求新增简单页面，检查新节点出现在无限画布。拖动画板、平移、缩放仍可使用。
7. 执行中点击停止，输入框恢复，页面不应被未应用方案改变。重新发送可再次运行。
8. 新建对话，切换历史记录，重命名标题。输入未发送的草稿，刷新后检查草稿、消息与已应用页面仍在。
9. 执行中刷新，旧运行应显示中断，不永久转圈。较旧方案在画布版本变化后不得覆盖新版本。
10. 断网、错误密钥、模型限流时应明确报错，不假装成功；已保存页面不丢失。

## 无限画布回归（2026-09-08）

- 保持聊天框、侧栏、画板工具栏原有样式；导入页恢复原批注输入框、编号、确认、清空和批量提交。
- “修改 → 添加评论”走同样的节点选取；评论、回复、解决状态与对话保存在同一项目本地状态，画板显示坐标标记。
- 未固定侧栏不应打断元素选择；评论提交或定位应打开评论栏。批注操作栏根据两侧实际宽度定位，不压在聊天框上。
- 组件引用保留“直接替换 / 圈选批注 / 框选批注”。引用包含页面和目标节点，退出应清除箭头及高亮。
- 导入页的“基于此页生成新方案”仅加入 AI 请求，不立即覆盖原页面；仍需发送、模型生成和手动应用。
- 导入页画板位置与缩放保存到当前项目；空白拖动平移，空格拖动平移，滚轮平移，Ctrl/Command + 滚轮缩放。浮层内滚动不平移画布。
- 失败回复重试保留当前未发送草稿和新引用。

本轮浏览器已实测：导入页修改/评论元素选择、就地输入与确认、清空、评论提交、重新进入项目后的评论保留、删除临时评论、组件引用目标选择与范围菜单入口。
构建及差异格式检查通过。框选/圈选完整提交、全部菜单的端到端回归及真实模型应用仍需补充验收，不能把入口可用等同于完整功能通过。

## 当前边界

- 支持读取当前项目、检查页面、修改节点文案/布局样式、新增结构化 React DOM 页面。
- 工具执行记录是真实动作摘要，不展示模型的隐藏思维链。
- 自由运行代码、业务接口生成、自动读取外部 Paper/Figma、完整组件资产检索不在本轮工具范围中。
- 对话和页面按导入文件、画板和版本隔离，保存在当前浏览器本地；这不是团队云端同步。完整跨设备交付还需账号、共享数据库和部署。
- 预览服务仅绑定本机。客户在自己的环境运行时需配置自己的模型服务。
- 原导入缓存保留；Agent 编辑写入独立版本状态。已有 Figma 还原的视觉缺陷不属于聊天功能修复。
- 撤回以最近已应用版本为边界，避免覆盖后续修改。

## 自动检查

```sh
node --test server/agent.test.mjs
```

测试使用隔离的本地模型协议夹具，验证真实 HTTP、流式工具循环、取消、错误、数据校验与原子应用，不代表真实模型的效果验收。真实模型验收仍须完成上面的连接与自然语言步骤。

实现协议依据：[OpenAI Function Calling](https://developers.openai.com/api/docs/guides/function-calling)、[Streaming Responses](https://developers.openai.com/api/docs/guides/streaming-responses)。
