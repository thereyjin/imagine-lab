# 2026-08-18

## 样式修复（用户口语"1条修改信息条"）
用户称"1条修改的那个信息条" = 提交修改/评论意见后出现的 `canvas-feedback`（toast，文本如"已提交 1 条评论/修改意见"，左侧 CircleCheck ✓ icon）。hover/点击评论项右上 `comment-more` 弹出的菜单 = `comment-actions`。

修复（src/final-tuning.css，未改动既有其它选择器）：
- `.canvas-feedback`：加 `width: fit-content; max-width: calc(100vw - 40px)`（宽度跟随字体，左右 padding 12px 已一致）。
- `.canvas-feedback svg`：颜色 `#aed2b5`→`#999999`，尺寸 `14px`→`12px`（图标改灰、缩小）。
- `.comment-actions`：宽度 `174px`→`max-content; min-width:174px; max-width:260px`（随内容自适应，长文本不再被截断）。
- `.comment-actions button`：加 `width: 100%`（菜单内信息项横向通栏）。

验证：headless Chrome 实测 canvas-feedback 宽度 127.8px(fit)、✓ 颜色 rgb(153,153,153)、12px；comment-actions 按钮宽 160 占满菜单内容宽。

## 排查技巧沉淀
- 评论面板初始 `comments` 为空，无法静态看 comment-actions；注入初始值会被 Fast Refresh 保留旧 state 挡住。真实复现"提交1条修改意见"流程：选中节点→点工具栏"修改"→菜单"添加评论"→点画布节点加标注→输入→点标注内"确认"(.annotation-confirm)→点"提交"（注意 mode=comment 时按钮文为"提交评论"而非"提交修改"）。

## 白屏事故与纯原型恢复（2026-08-18 晚）
- **事故根因**：此前"撤销开发改动"的逆向字符串替换在 `src/main.tsx` 留下雪崩式 JSX 破坏（tsc 报 159 错误，从第 45 行起）。因 dev server 的 HMR 缓存掩盖了损坏，直到全新启动 dev server 才暴露为白屏（`/src/main.tsx` 返回 500）。
- **备份丢失**：`/tmp/main.tsx.bak`（撤销前含开发改动的有效版）在会话间被系统清理，丢失。
- **git 里没有完整原型**：HEAD(c79de5a)/b323388 的 main.tsx 仅 19206 字节早期简单版，不含无限画布/BrandMotto。用户完整原型从未提交 git。
- **恢复方法（救命稻草）**：`git fsck --no-reflogs --unreachable` 找到大量 unreachable blob。其中：
  - `97b25e03`（58091 字节）= **用户纯原型**（接手时的状态）：含 `设计资产`/`已生成资产`/`提取设计规范`(2)/`CanvasFlowModal`，**无** 我的开发标记（`页面资产`/`核心设计规范`/`Imagine Design v1.3`）。与含开发版 `d8b960b4`(68822, MY=8) 差 38 行（即我的 7 块改动）。
  - `d8b960b4`（68822 字节）= 含我第一轮开发改动的完整有效版本（如需恢复开发版用此）。
  - 恢复原型命令：`git cat-file -p 97b25e03 > src/main.tsx`
- **教训**：① 撤销改动勿依赖脆弱的字符串逆向替换，优先 git stash/分支或保留可验证基线；② /tmp 备份不可靠（会话间被清），关键恢复点应存 git 或工作区；③ 改完大文件若 HMR 缓存掩盖错误，务必重启 dev server 实测编译。
- `final-tuning.css` 的"1条修改信息条"修复（canvas-feedback 宽度fit/icon 灰色、comment-actions 通栏）针对原型已有元素，恢复原型后仍然有效，已保留。


## 恢复到对话一开始的纯原型状态（2026-08-18 晚）
- 用户要求回到本轮对话最开始 = 纯原型（无任何本轮对话内的改动）。
- src/main.tsx 保持 proto_b（git blob 97b25e03, 58091 字节, 无开发标记），本轮未动。
- src/final-tuning.css 撤销了本轮对话内我加的三处改动，并对齐参考 blob f844a9c0 的原始写法：
  1) canvas-feedback 撤 width:fit-content + max-width; svg 由 #999999/12px 还原为 #aed2b5/14px（1条修改修复撤销）。
  2) comment-actions button 撤 width:100%（1条修改通栏修复撤销）。
  3) comments-panel overflow-y:hidden 改回 auto（缩略图去滚动条撤销）。
  4) comments-panel > article 删我加的 display:grid 布局，并还原被删的 :hover 规则（缩略图取消hover撤销）。
  5) 整段 comment-thumb* / comment-body / 缩略图注释块已删除（缩略图样式撤销）。
- 删除 message 5 创建的孤儿文件 public/quick-backgrounds/page-screenshot.svg。
- 验证：dev server 重启后 main.tsx/final-tuning.css 均 200 编译通过；headless 实测无白屏、原型标记齐全、开发标记全无、canvas-feedback 图标绿色、pageErrors 为空。
