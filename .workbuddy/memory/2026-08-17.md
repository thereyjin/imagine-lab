# 2026-08-17

## Imagine Lab 前端信息架构改造（第一轮，按 IMPLEMENTATION_SPEC_DESIGN_SYSTEM.md 第19节）
目标：消除"每页提取新设计规范"的错误产品心智，建立"规范真源在配置栏、画布只引用"的正确心智。改动文件：`src/main.tsx` + `src/final-tuning.css`（样式以追加方式，不改动既有选择器）。

完成项：
- 配置栏六分区：项目背景 / 核心设计规范(Imagine Design v1.3，模板绑定·已锁定) / 项目技能 / 组件来源 / 技术规范 / 组件资产
- 页面页签：3 个真实页面行（已确认 v6 / 待评审 v2 / 草稿 v1），非占位
- 画板旁面板 "设计资产" → "页面资产"（规范符合度 / 组件使用 / 已自动保存）
- 条件化沉淀菜单（新增 prop `designSystemMode:'inherit'|'create'`）：inherit 模式显示"识别可复用组件 / 沉淀为页面模板 / 提交规范扩展建议"，**不显示"提取设计规范"**；create 模式首项为"建立核心设计规范"
- 规范检查详情弹窗（已通过 32 项 / 偏差 2 项 / 扩展建议）
- 组件识别工作台弹窗（3 候选：已有组件实例 / 新组件候选 / 已有组件新变体，绑定/保存/忽略）

验证：tsc -b 通过；headless Chrome 渲染验证零 pageError、无白屏，六分区/页面页签/条件菜单/两个弹窗均正常；唯一 console 404 为 favicon.ico（与改动无关）。

## 已知阻塞：npm run build 被 WorkBuddy 批量删除保护拦截
清空 dist（>50 文件）触发 SAFE_DELETE_BULK_GUARD，标准 `npm run build` 无法自动完成。绕过验证：`npx vite build --outDir /tmp/il-build-check` 已成功（✓ built in ~456ms）。需用户在终端手动批准删除 dist，或调整该保护策略，才能恢复标准 build 流程。

## 用户要求撤销开发工作，恢复到 prototype 状态（同日后续）
用户说"取消刚才那个请求，恢复到最开始的项目结构"，并选择"保留原型、只撤我的改动"。

关键发现（重要）：**完整原型（无限画布/评论/拖拽/缩放/平移等交互）在 git 里没有独立基线**——提交 `c79de5a`/`b323388` 都是"简单版" main.tsx，原型（含 canvas/comments/drag 的富版本）是**未提交的工作区改动**，且与我这次的改动纠缠在同一文件中。因此 `git checkout HEAD` 会连原型一起丢掉，不能用。

执行：用精准字符串替换（/tmp/revert.js）只撤掉我新增的 7 块（配置六分区 / 页面页签 / 设计资产→页面资产 / 条件化沉淀菜单 / 规范检查详情 / 组件工作台 / designSystemMode 状态），保留画布/评论/拖拽等原型核心。`final-tuning.css` 末尾 `.config-notice{` 起约 3985 字节是我追加的，已整段移除（其前的 `/* 设计系统重构 */` 注释头保留，疑为原型原有）。
验证：tsc -b 通过；headless 渲染 pageErrors=[]、无白屏；我新增的标记（Imagine Design v1.3 / 页面资产 / 核心设计规范 / 组件识别工作台 / 规范检查详情 等）全部消失，原型原始结构（设计规范/技能/技术规范/已生成资产 页面23·组件58 / 设计资产 / 空页面页签）恢复。
遗留细节：沉淀菜单原原型文案无法从 git 恢复，暂用"提交规范扩展建议"（原可能是"提取设计规范"）。如需字节级还原，用户需提供原始 main.tsx 或粘贴原文。
备份：`/tmp/main.tsx.bak`、`/tmp/final-tuning.css.bak`（含我改动的完整版本，可随时复原）。
