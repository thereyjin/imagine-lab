# Imagine Lab｜AI 接管与持续开发说明书

> 文档用途：这是 Imagine Lab 当前阶段的单一事实源（Single Source of Truth）。新的 AI、设计师或开发者在修改任何代码前，必须完整阅读本文，再读取当前代码与 Git 状态。本文用于避免错误目录、错误预览、擅自重构、视觉跑偏、把演示逻辑误认为真实功能等问题。
>
> 当前文档版本：2026-08-12
>
> 当前项目目录：`/Users/sevenjin/Desktop/imagine lab`
>
> 当前 GitHub 仓库：`https://github.com/thereyjin/imagine-lab`
>
> 当前主分支：`main`
>
> 编写本文档时的产品代码提交：`c79de5a7901d0776dfd5726c8560c9da0c6dc018`
>
> 设计规范、技能、组件、页面与技术规范的最新重构结论，以 `IMPLEMENTATION_SPEC_DESIGN_SYSTEM.md` 为准；执行相关改造前必须完整阅读该文件。

---

## 0. 给接管 AI 的最短指令

如果你是刚接手该项目的 AI，请严格按以下顺序执行：

1. 确认绝对路径是 `/Users/sevenjin/Desktop/imagine lab`，文件夹名是 **imagine lab**，不是 `imagine ai`。
2. 执行 `pwd`、`git status -sb`、`git log -3 --oneline`，先确认目录和工作区状态。
3. 完整阅读本文件、`IMPLEMENTATION_SPEC_DESIGN_SYSTEM.md`、`package.json`、`src/main.tsx`、`src/styles.css`、`src/precision.css`、`src/final-tuning.css`。
4. 启动前先检查端口占用；运行 `npm run dev -- --host 127.0.0.1`，打开终端实际返回的 URL，不要想当然地打开某个端口。
5. 页面若出现“故事工作室、语音生成、Ethan、时间轴”等内容，说明打开了错误项目或错误端口；这些内容从未属于 Imagine Lab。
6. 用户会通过浏览器标注逐项微调。每次只修改标注指定区域，除非用户明确要求，不得顺手改变其他区域。
7. 修改前先定位现有组件和 CSS 覆盖关系；不要重做已经完成的页面，不要替换用户已经确认的参数。
8. 修改后至少运行 `npm run build`；涉及视觉时，还要在浏览器中以当前页面实际状态复核。
9. 不要把当前模拟动画或本地 state 描述成真实 AI、MCP、Computer Use、数据库或多人协作能力。
10. 不要擅自提交或推送 GitHub；只有用户明确要求同步、提交或发布时才操作远端。

---

## 1. 产品一句话定义

**Imagine Lab 是面向产品团队的 AI 设计协作系统，让产品需求、产品理解、设计规范、团队技能、可复用资产、AI 协作和最终交付进入同一条连续工作流。**

产品口号：

> 让想象力成为团队的生产力。

Imagine Lab 不是单纯的 AI 出图工具，也不是只做 UI 生成的画布。它的核心价值是：让 AI 理解一个产品是什么、服务谁、解决什么问题、如何解决，并将这种产品理解持续带入后续的设计、规范沉淀、资产复用、协作和交付。

---

## 2. 产品愿景与最终目标

### 2.1 愿景

让产品团队的想象力不再散落在会议、文档、原型、聊天记录和个人经验里，而是沉淀成团队能够调用、复用、演进和交付的生产力。

### 2.2 最终目标

最终的 Imagine Lab 应覆盖以下完整周期：

1. **理解产品**：读取产品资料，建立产品定位、目标用户、核心问题、解决方案和业务逻辑。
2. **规划需求**：把模糊需求转化为明确的产品目标、范围、页面和任务。
3. **生成与迭代设计**：在项目画板中通过 AI 主线/分支协作生成、比较和迭代页面方案。
4. **遵循设计规范**：自动应用团队已有的颜色、字体、间距、组件和交互规则。
5. **沉淀团队技能**：把成熟的工作方法、提示策略、组件构建方法等封装为可复用 Skill。
6. **复用团队资产**：复用组件、页面模板、品牌资产、图标、文案、设计规范和历史项目经验。
7. **多人协作**：支持评论、讨论、分支、版本、团队分享和可追溯的工作记录。
8. **交付实现**：输出可继续开发的前端代码、规范、资产和项目说明，而不是只输出一张图片。

### 2.3 长期体验原则

- AI 必须融入产品设计工作流，而不是成为一个悬浮在外部的聊天机器人。
- 用户始终能看到 AI 正在做什么、进行到哪一步、基于什么资料做出结论。
- AI 的输出应可回溯、可分支、可比较、可编辑、可复用。
- AI 应优先使用团队已有规范、技能和资产，避免每次从零生成。
- 界面要克制、专业、轻量、高品质，有品牌辨识度，但不能喧宾夺主。
- 产品应帮助团队减少重复劳动，不以增加配置负担为代价。

---

## 3. 目标用户与核心场景

### 3.1 主要用户

- 产品经理：整理产品目标、需求、业务逻辑和页面范围。
- 产品设计师 / UI 设计师：生成和迭代页面、管理规范与组件。
- 设计负责人：维护团队设计标准、技能和资产质量。
- 前端工程师：获取结构清晰、规范一致、可继续实现的代码与资产。
- 小型产品团队：希望在人员较少的情况下缩短从想法到可交付原型的时间。

### 3.2 核心痛点

- 产品资料散落在飞书、Notion、Obsidian、蓝湖、墨刀和本地文件中。
- AI 往往只看到一句需求，不理解产品背景，导致生成结果表面正确、实际偏题。
- 设计规范、组件和团队经验无法稳定进入 AI 上下文。
- AI 生成过程缺乏可控的主线、分支和版本关系。
- 设计输出和代码实现断层，设计资产难以持续复用。
- 团队成员各自使用 AI，成果不能形成组织资产。

### 3.3 典型用户旅程

1. 用户进入首页。
2. 通过“原型分析”连接产品资料，让 AI 先理解产品。
3. 从模板、快速开始或已有项目进入项目画板。
4. AI 在画板底部输入区参与主线和分支讨论。
5. AI 根据产品资料、设计规范、Skill 和资产生成或修改页面。
6. 用户在右侧面板调整配置、切换页面、查看评论。
7. 团队通过评论、分享、工作记录和版本关系协作。
8. 最终导出代码、资产和交付说明。

---

## 4. 产品信息架构

### 4.1 全局导航

当前左侧统一导航包含：

- 工作区切换：`UI设计`
- 首页
- 项目
- 资产库
- 产品分组
  - 设计规范
  - 技能库
  - 连接器
  - 团队设置
- 底部 API 设置与额度进度

未来这些导航应该逐步成为真实页面，但当前只有首页和项目画板具备实现。

### 4.2 首页

首页承担“开始工作”和“恢复工作”的作用，结构为：

- 顶部导航
  - 侧边栏展开/收起入口
  - 消息/评论入口
  - 当前团队：`想象力产品部`
- 四张快捷入口卡片
  - 原型分析
  - 从模板创建
  - 快速开始
  - 导入资产
- 最近项目
- 工作记录
- 团队分享
- 侧边栏底部 API 设置与额度

### 4.3 项目画板

项目画板是未来的核心工作空间，由以下区域组成：

- 与首页一致的统一侧边栏和顶部导航
- 左侧项目列表
- 中间点阵式无限画布
- 底部 AI 聊天/任务输入框
- 主线与分支选择
- 右侧面板
  - 配置
  - 页面
  - 评论
- 导出入口

### 4.4 原型分析

“原型分析”不是按“设计原型”和“项目文档”分类，而是按 **AI 的读取方式** 分类。用户不需要理解底层连接细节，选定来源后，后续连接、读取、过滤和提炼过程由系统自动完成。

读取目标只包括产品内容：

- 产品是什么
- 服务谁 / 目标用户是谁
- 解决什么问题
- 如何解决
- 产品定位与价值主张
- 业务规则、业务对象和关键流程
- 产品范围、约束、重要术语

明确不读取或不进入产品分析的内容：

- UI 视觉样式
- 组件尺寸和视觉参数
- 交互标注
- 页面布局细节
- 前端代码
- 动画实现
- 与产品理解无关的截图内容

---

## 5. 当前已实现的前端原型

以下内容是当前代码中真实存在的实现。

### 5.1 首页

已实现：

- 完整首页布局。
- 统一左侧导航。
- 点击顶部面板图标展开/收起侧边栏。
- 四张快捷入口卡片。
- 快捷卡片使用暖白低对比插图、前端图标和文字叠加。
- 插图通过 Alpha Mask 淡出，避免影响文字可读性。
- 最近项目卡片及成员头像展示。
- 工作记录列表。
- 团队分享杂志感封面。
- API 设置/额度静态展示。
- 点击“原型分析”打开弹窗。
- 点击“从模板创建”或最近项目进入项目画板。

尚未实现：

- 首页数据接口。
- 真实项目读取与创建。
- 搜索、排序和分页。
- 真实工作记录。
- 真实团队分享内容。
- API 配置和额度计算。
- 除原型分析、从模板创建以外快捷入口的完整流程。

### 5.2 项目画板

已实现：

- 首页与项目页共用的侧边栏和顶部导航。
- 左侧项目列表。
- 项目列表展开/收起。
- 项目行 hover 时显示三点按钮。
- 项目三点菜单。
- 点击画布空白区域关闭菜单。
- 中间点阵式画布的视觉框架。
- 底部 AI 输入框视觉框架。
- 主线/分支弹窗。
- 右侧配置、页面、评论页签。
- 右侧配置面板的静态字段与导出入口。
- 项目画板各区域被包裹为一个统一连续编辑表面。

尚未实现：

- 真正的无限画布能力（缩放、平移、节点、框选、对齐、连线）。
- 画布页面渲染和编辑。
- AI 对话接口。
- 消息流、流式输出、停止生成、重试。
- 主线/分支的数据模型、历史和合并。
- 项目列表的真实增删改查。
- 页面、评论和配置的真实业务逻辑。
- 版本系统、多人在线、协作光标。
- 导出代码或项目包。

### 5.3 原型分析弹窗

当前实现包含三个读取方式：

1. **直接读取本地资料**
   - 当前展示来源：Obsidian。
   - 预期能力：选择本地资料，读取 Markdown 与文本内容。
2. **连接在线文档**
   - 当前展示来源：飞书、Notion。
   - 点击“继续”后先进入文档产品选择页。
   - 预期能力：用户授权后自动定位并读取相关产品文档。
3. **浏览器辅助读取**
   - 当前展示来源：蓝湖、墨刀。
   - 预期能力：无法通过开放接口或连接器读取时，调用 Computer Use/浏览器辅助能力，只提取产品文档文本。

当前交互：

- 点击读取方式后显示黑色选中描边。
- 右上角显示黑色圆形勾选标记，勾图标居中。
- 三种选项的左上角图标使用统一线性风格和固定布局，不应在选择时跳动。
- 只有选中读取方式后，“继续”按钮才可用。
- “继续”按钮当前尺寸为 `292 × 36px`，胶囊形。
- 在线文档会先要求选择飞书或 Notion。
- 选择后进入“正在分析产品原型”状态。
- 分析状态以类似大模型聊天的方式显示“正在思考”和步骤进度。
- 模拟步骤会按照约 1.15 秒间隔推进。
- 可按 `Escape` 关闭弹窗。
- 点击遮罩空白区域关闭弹窗。

重要事实：

- 当前步骤推进只是 `setInterval` 驱动的前端演示。
- 当前没有文件选择器。
- 当前没有 OAuth。
- 当前没有 MCP。
- 当前没有 Computer Use 调用。
- 当前没有真实读取或产品分析结果页。
- 官方来源 Logo 已使用本地资源，而不是文字占位图标。

---

## 6. 原型分析的产品与技术设计

这一部分是后续实现时必须遵循的目标设计。

### 6.1 为什么按读取方式分类

用户的目标不是选择一种“文档类型”，而是让 AI 理解产品。不同平台的真正差异在于系统如何获得文本：

- 本地文件可以通过文件选择或文件系统权限读取。
- 开放在线文档可以通过 MCP、OAuth、官方 API 或连接器读取。
- 封闭平台在没有可用接口时，需要通过授权后的浏览器自动化或 Computer Use 读取。

因此前端只需要让用户选择来源，连接细节由系统在后台自动规划。

### 6.2 推荐读取管线

统一处理流程：

1. 识别来源和读取能力。
2. 获取用户授权。
3. 定位用户指定的工作区、文件夹、项目或文档。
4. 建立候选文档清单。
5. 优先读取标题、目录和纯文本，避免一开始加载图片与复杂视觉树。
6. 按相关性筛选产品文档。
7. 提取产品正文。
8. 过滤 UI、交互标注、代码和重复内容。
9. 形成结构化产品事实。
10. 标记事实来源、置信度、冲突和未知项。
11. 生成产品分析摘要，并允许用户修正。
12. 将确认后的产品理解保存为项目长期上下文。

### 6.3 统一结构化输出建议

后端最终应输出类似以下结构：

```ts
type ProductUnderstanding = {
  productName?: string;
  oneLineSummary?: string;
  positioning?: string;
  targetUsers: Array<{
    name: string;
    needs: string[];
    pains: string[];
  }>;
  problems: Array<{
    statement: string;
    evidence: SourceReference[];
    confidence: number;
  }>;
  solutions: Array<{
    statement: string;
    addressesProblemIds: string[];
    evidence: SourceReference[];
  }>;
  valuePropositions: string[];
  businessObjects: string[];
  keyFlows: string[];
  constraints: string[];
  terminology: Record<string, string>;
  openQuestions: string[];
  conflicts: Array<{
    topic: string;
    statements: string[];
    sources: SourceReference[];
  }>;
};
```

### 6.4 本地资料读取

首期支持建议：

- 文件：`.md`、`.mdx`、`.txt`。
- 文件夹：递归扫描，但默认忽略隐藏目录、构建产物和代码依赖。
- Obsidian：本质上读取 Vault 内的 Markdown、双链和 frontmatter。
- 可选后续支持：PDF、DOCX，但要单独做文本提取和质量提示。

默认忽略：

- `node_modules`
- `.git`
- `dist`
- 图片和视频
- 大型二进制附件
- 代码文件
- 未被正文引用的缓存目录

### 6.5 飞书 / Notion 在线文档

推荐策略：

- 优先使用现成且受支持的 MCP/官方连接器。
- 如果没有 MCP，再使用官方 OAuth + API。
- 授权完成后让用户选择工作区或起始文档，不要默认遍历整个账号。
- 先读取文档树和元数据，再按需读取正文。
- UI 只展示用户能理解的进度，不暴露内部工具调用细节。

前端状态建议：

- 等待授权
- 正在连接
- 正在定位资料
- 正在读取正文
- 正在提炼产品事实
- 等待用户确认
- 已完成
- 权限不足 / 连接失效 / 未找到内容

### 6.6 蓝湖 / 墨刀浏览器辅助读取

只有在无稳定 API、无 MCP、无法导出可解析文本时才使用 Computer Use。

节约 token 的原则：

- 用户先提供精确项目链接或项目名称。
- 先读取文档目录，不遍历所有设计页面。
- 只进入需求说明、产品说明、备注、文档正文等文本区域。
- 不做逐像素截图分析。
- 不读取 UI 组件树、颜色、尺寸、切图和代码标注。
- 对长文档分块读取并去重。
- 每个步骤保存已提取文本，避免反复打开同一页面。
- 遇到验证码、登录、敏感授权必须交还用户操作。
- 读取完成后明确列出读取范围和跳过范围。

建议未来为此设计一个专用 Skill，目标只有：

> 在受限平台中，以最少页面访问、最少截图和最少 token，提取与产品定位、用户问题、业务逻辑、解决方案有关的纯文本。

---

## 7. 当前 UI 视觉规范

### 7.1 整体气质

关键词：

- 克制
- 专业
- 高品质
- 轻量
- 温和
- 低对比
- 有品牌辨识度
- 细腻但不装饰过度

避免：

- 高饱和大面积渐变。
- 过重阴影。
- 夸张动效。
- 大量玻璃拟态。
- 游戏化发光效果。
- 粗黑大标题。
- 不统一的图标风格。
- 为“AI 感”随意添加紫色霓虹。

### 7.2 字体

当前全局字体栈：

```css
font-family: Onest, "PingFang SC", system-ui, sans-serif;
```

说明：项目当前没有打包 Onest 字体文件，因此若系统没有该字体，会回退到苹方或系统字体。

使用规则：

- 常用导航与正文：`14px`。
- 次级说明：`12–13px`。
- 极弱辅助状态：`10–11px`。
- 区块标题常用：`18px`。
- 弹窗标题：`20px / 28px`。
- 常用字重：`400`、`500`。
- 尽量不使用 `600/700`，除非具有明确层级必要性。
- 中文界面统一，不要在同层级随意混用中英文。

### 7.3 核心颜色

当前代码中高频色值：

| 用途 | 色值 |
|---|---|
| 主文字 | `#0f0e0d` |
| 深色按钮 | `#171717` / `#24211f` |
| 次级正文 | `#504b49` |
| 辅助文字 | `#857e7b` |
| 弱提示文字 | `#999` / `#aaa39f` |
| 页面暖白背景 | `#fdfcfc` |
| 编辑区暖白 | `#fbfaf9` |
| 浅暖灰背景 | `#f3eeec` / `#f5f3f1` |
| 常用边框 | `#e4e4e7` / `#e9e5e3` / `#ece8e6` |
| hover 背景 | `#f1f1f1` |
| 在线状态 | 背景 `#eef8f1`，文字 `#388a57` |

注意：未来应把色值整理为 CSS Variables / Design Tokens，但在用户完成视觉确认前，不要机械地替换所有色值导致渲染变化。

### 7.4 圆角

- 小按钮：`6–10px`。
- 普通控制：`8–14px`。
- 卡片：`12–17px`。
- 大面板/编辑表面：`24px`。
- 胶囊按钮：`999px`。
- 圆形图标：`50%`。

### 7.5 边框与阴影

- 边框通常为 `1px`，暖灰、低对比。
- 面板主要依靠边框和背景区分，不依赖重阴影。
- 弹窗可使用较大但柔和的投影：当前为两层暖黑透明阴影。
- hover 阴影只做轻微层级提升。
- 选中态优先使用深色细描边，而不是强烈背景填充。

### 7.6 图标

- 通用图标主要来自 `lucide-react`。
- 风格为统一线性图标。
- 常用尺寸 `14–20px`。
- 常用描边 `1.5–2`。
- 同一组图标必须保持视觉重量和占位尺寸一致。
- 选择状态不得引起图标或文本位移。
- 文档产品必须使用对应的官方真实 Logo。
- 当前飞书、Notion、Obsidian、蓝湖、墨刀 Logo 位于 `public/source-logos/`。

### 7.7 动效

- 侧边栏宽度：约 `180ms ease`。
- 弹窗进入：`160–180ms`。
- hover：约 `120–150ms`。
- 动效用于表达状态变化，不做持续装饰。
- 选中描边变化时不得造成布局跳动。
- 加载状态可使用旋转图标和低调的三点动画。

### 7.8 页面布局关键参数

当前重要尺寸：

- 展开侧边栏：`224px`。
- 收起侧边栏：`64px`。
- 顶部栏：`52px`。
- 首页团队分享列：约 `380px`。
- 项目列表：约 `240px`，窄屏为 `220px`。
- 右侧配置栏：约 `321px`，窄屏为 `300px`。
- 底部输入框：`628 × 144px`，窄屏为 `560px` 宽。
- 点阵画布网格：`16 × 16px`。
- 原型分析弹窗：默认最大约 `580px`，处理中最大约 `600px`。
- 原型分析选项：最小高度 `104px`。
- 原型分析选中勾：`20 × 20px`，位于选项 `top: 16px; right: 16px`。
- 原型分析选项主图标占位：`24 × 24px`，SVG 约 `21 × 21px`。
- 原型分析继续按钮：`292 × 36px`。

### 7.9 首页快捷卡片

- 四张卡片统一结构和比例。
- 插图是暖白、低对比背景，不是独立内容图。
- 图标和文字由前端渲染，不能烘焙进位图。
- 插图通过遮罩从上部图像自然淡出到文字区域。
- 卡片文字必须始终比插图更清晰。
- 当前最终使用的资源为 `public/quick-backgrounds/*-20260812-v2.png`。
- `public/quick-backgrounds/` 中存在多轮历史版本，未确认前不要批量删除。

### 7.10 团队分享封面

- 采用高品质杂志感封面。
- 封面可有更明显的图像表现，但必须服从整体低对比、专业气质。
- 当前资源位于 `src/assets/share-covers/`。

---

## 8. 技术架构现状

### 8.1 技术栈

- React
- TypeScript
- Vite
- CSS
- `lucide-react`

当前依赖版本在 `package.json` 中使用 `latest`，锁文件用于固定实际安装结果。

### 8.2 当前目录结构

```text
imagine lab/
├── AI_HANDOFF.md              # 本交接文档
├── README.md                  # 当前只有项目名和口号
├── index.html                 # Vite 入口
├── package.json
├── package-lock.json
├── pnpm-lock.yaml
├── tsconfig.json
├── public/
│   ├── quick-backgrounds/     # 首页快捷卡片插图，多轮候选版本
│   └── source-logos/          # 文档平台官方 Logo
└── src/
    ├── main.tsx               # 当前全部 React 组件、数据和状态
    ├── styles.css             # 初始基础样式，压缩为单行
    ├── precision.css          # 第二轮精修覆盖，压缩为少量长行
    ├── final-tuning.css       # 当前最终视觉覆盖，约 2000 行
    ├── vite-env.d.ts
    └── assets/
        ├── quick-backgrounds/
        └── share-covers/
```

### 8.3 当前组件

`src/main.tsx` 中主要组件：

- `App`
- `Home`
- `Project`
- `HomeSidebar`
- `Header`
- `Logo`
- `Avatar`
- `TeamPill`
- `PrototypeAnalysisModal`
- `SourceList`
- `Title`
- `Record`
- `Share`
- `ProjectList`
- `DotCanvas`
- `Composer`
- `ConfigPanel`
- `Config`
- `Row`

代码目前高度集中，`main.tsx` 只有约 58 个物理行，但许多组件被写在超长单行中。这是原型期实现，不是推荐的长期结构。

### 8.4 当前状态管理

全部使用 React 本地 `useState`：

- 页面：`home | project`
- 侧边栏展开/收起
- 原型分析弹窗开关
- 原型分析读取方式
- 在线文档来源选择
- 模拟分析步骤
- 项目三点菜单
- 项目列表展开/收起
- 主线/分支菜单
- 右侧页签

没有全局状态库，没有 URL 路由，没有持久化。

### 8.5 当前样式层级

样式导入顺序：

1. `styles.css`
2. `precision.css`
3. `final-tuning.css`

后导入样式覆盖前面的样式。当前大量最终参数依赖 CSS cascade，因此：

- 调整某个选择器前要用浏览器计算样式确认最终生效值。
- 不要只修改 `styles.css` 后就认为会生效。
- 不要贸然合并 CSS 文件，除非先建立视觉回归基线。
- `final-tuning.css` 中可能存在同一选择器的多次覆盖，这是历史逐项精修留下的结果。

### 8.6 当前路由与数据

- 没有 React Router。
- `App` 通过本地 state 在首页和项目页之间切换。
- 项目、记录、分享、配置都使用静态数组或静态 JSX。
- 刷新页面会回到首页。
- 不支持深链接。

### 8.7 当前后端能力

目前不存在：

- 服务端项目。
- REST/GraphQL API。
- 数据库。
- 用户认证。
- 文件存储。
- OAuth。
- WebSocket。
- AI 模型调用。
- MCP 调用。
- Computer Use 调用。
- 队列或异步任务系统。
- 日志、监控、分析埋点。

### 8.8 外部资源风险

`src/main.tsx` 中部分 Logo、头像和装饰资源引用 `app.paper.design` 的远程 URL。这些 URL 可能失效，也会造成离线预览不完整。

后续建议：

1. 在确认版权和使用范围后，将必要资源下载为本地资产。
2. 使用明确文件名和用途目录。
3. 替换远程 URL 后逐项视觉核对。
4. 不要在没有对比截图时一次替换全部资源。

---

## 9. 本地运行与验证

### 9.1 环境

建议：

- Node.js 20 或更新的稳定版本。
- npm 或 pnpm 二选一，但一次任务中不要混用安装命令。

仓库同时存在 `package-lock.json` 和 `pnpm-lock.yaml`。当前可以使用 npm；未来团队应明确唯一包管理器后再清理另一份锁文件。

### 9.2 启动

```bash
cd "/Users/sevenjin/Desktop/imagine lab"
npm install
npm run dev -- --host 127.0.0.1
```

通常预览地址为：

```text
http://127.0.0.1:5173/
```

但必须以 Vite 终端实际输出为准。如果 5173 已占用，Vite 可能自动使用其他端口。

### 9.3 启动前端口检查

```bash
lsof -nP -iTCP:5173 -sTCP:LISTEN
```

如果端口已被占用，要检查进程工作目录，不要直接打开已有服务：

```bash
lsof -a -p <PID> -d cwd -Fn
```

### 9.4 构建验证

```bash
npm run build
```

该命令会执行：

```text
tsc -b && vite build
```

### 9.5 每次修改后的最低验收

- `npm run build` 通过。
- 浏览器没有白屏。
- 控制台没有新增错误。
- 首页和项目画板仍可切换。
- 侧边栏仍可展开/收起。
- 原型分析弹窗仍可打开、选择、继续、返回和关闭。
- 指定区域符合用户标注。
- 未指定区域没有视觉回归。

---

## 10. Git 与协作规则

### 10.1 当前远端

```text
origin https://github.com/thereyjin/imagine-lab.git
```

默认 GitHub 账号：`thereyjin`。

### 10.2 修改前

```bash
git status -sb
git log -3 --oneline
```

- 先识别用户已有的未提交修改。
- 不覆盖、不重置、不删除不属于当前任务的改动。
- 不使用 `git reset --hard`。
- 不使用 `git checkout --` 恢复用户文件，除非用户明确授权。

### 10.3 修改后

- 先构建和视觉验证。
- 查看 `git diff --check`。
- 查看 `git diff --stat` 和关键差异。
- 只有用户明确要求时才 commit/push。

### 10.4 忽略内容

`.gitignore` 当前排除：

- `node_modules/`
- `dist/`
- TypeScript 构建缓存
- `.DS_Store`
- 环境变量文件
- 包管理器 debug 日志

不要提交真实密钥、OAuth Token、API Key 或用户文档内容。

---

## 11. 绝对不能跑偏的约束

### 11.1 产品约束

- Imagine Lab 是 AI 设计协作系统，不是语音工作室、视频编辑器、传统项目管理工具或单纯的 AI 聊天工具。
- 原型分析的目标是理解产品，不是分析 UI 视觉和代码。
- 原型分析按读取方式分类，而不是按“设计原型/项目文档”分类。
- 蓝湖/墨刀只有在无更直接能力时才使用 Computer Use。
- AI 过程要可见，但不要向普通用户暴露冗长技术日志。
- 团队规范、技能和资产复用是核心能力，不是附属设置页。

### 11.2 视觉约束

- 严格参考用户指定的 Paper 画板。
- 浏览器标注优先级高于泛化审美判断。
- 只改指定区域。
- 不擅自统一所有圆角、颜色或间距。
- 不擅自引入新的品牌色。
- 不将线性图标替换成 emoji 或填充图标。
- 不用假 Logo 代替官方产品 Logo。
- 不让插图压过文字。
- 不因为“响应式优化”改变桌面版已确认布局。

### 11.3 工程约束

- 不重做已完成内容。
- 不把所有 CSS 重构作为某个小视觉任务的附带工作。
- 不新增大型依赖来解决简单 UI 问题。
- 不把模拟流程描述成真实功能。
- 不在未确认架构前直接接入某个供应商并将代码强耦合。
- 不读取、提交或上传用户真实产品文档作为测试数据。

---

## 12. 已确认的重要设计决策

1. 界面统一使用中文。
2. 14px 是常用导航和正文基准。
3. 字重主要使用 400/500。
4. 首页和项目画板共用统一侧边栏和顶部导航。
5. 首页四张快捷卡片使用暖白低对比插图。
6. 卡片插图、前端图标和文字分层实现。
7. 插图需要 Alpha Mask 淡出。
8. 团队分享使用高品质杂志感封面。
9. 项目画板中项目列表、点阵画布和右侧栏属于一个连续编辑表面。
10. 原型分析不区分“设计原型”和“项目文档”。
11. 原型分析按读取方式分类。
12. 原型分析只提取产品定义、问题和解决方案等产品内容。
13. UI、交互、代码即使存在于原资料中也不需要进入产品分析。
14. 用户选择来源后，连接与读取步骤由后台自动完成。
15. 前端用类似大模型聊天的方式显示思考与步骤进度。
16. 飞书/Notion 等开放来源优先通过连接器、MCP 或官方 API。
17. 蓝湖/墨刀等受限来源在没有更好办法时调用 Computer Use。
18. Computer Use 必须以快速、高效、节约 token 为目标，只读相关文本。
19. 文档产品使用官方真实 Logo。
20. 在线文档入口点击后必须先让用户选择连接哪个文档产品。
21. 原型分析选项的图标、选中描边、勾选标记、继续按钮曾按 Paper 画板重点精修。
22. 勾选图标必须在黑色圆圈中居中。
23. 切换选项时图标不得跳动。

---

## 13. 当前技术债与风险

### 高优先级

1. `src/main.tsx` 过度集中且存在超长单行，后续多人或多 AI 修改容易冲突。
2. CSS 由三层覆盖构成，最终值难以快速判断。
3. 没有自动化测试和视觉回归基线。
4. 没有真实路由，刷新无法保持页面。
5. 没有数据层、领域模型和接口边界。
6. 原型分析只有模拟进度，没有真实任务状态机。
7. 部分关键资源依赖 Paper 远程 URL。

### 中优先级

1. 同时存在 npm 和 pnpm 锁文件。
2. 依赖声明使用 `latest`，未来重新安装可能出现不一致。
3. 历史快捷卡片资源较多，占用仓库体积。
4. 缺少设计 token。
5. 缺少无障碍完整检查。
6. 响应式主要只覆盖约 1200px 和 620px 两个断点。

### 重构原则

重构必须分阶段，不得在用户要求局部 UI 微调时混入。

推荐顺序：

1. 先建立截图基线和关键交互检查清单。
2. 仅格式化/拆分 React 文件，保证 DOM 和 className 不变。
3. 建立路由和页面边界。
4. 建立类型、mock 数据和服务接口。
5. 再逐步整理 CSS token 和组件样式。
6. 每一步都做像素级回归。

---

## 14. 后续路线图

路线图按“先把用户能看到的核心流程做完整，再接真实能力，再扩展平台”排序。

### Phase 0：保护当前基线

目标：让任何后续开发都不会破坏已确认视觉。

任务：

- 保存首页、原型分析各状态、项目画板的基准截图。
- 建立关键交互人工验收清单。
- 记录常用视口尺寸。
- 明确 Paper 画板链接或导出参考图的存放位置。
- 将远程 Paper 资产逐步本地化。
- 确定唯一包管理器和 Node 版本。

完成标准：

- 新 AI 能在 5 分钟内启动正确页面。
- 每个核心页面都有可对照基线。
- 构建与预览步骤稳定。

### Phase 1：完成“原型分析”端到端前端体验

目标：即使先使用 mock，也要把真实产品流程所有状态设计完整。

任务：

- 本地资料选择器。
- 在线文档来源选择。
- OAuth 等待和回调状态。
- 浏览器辅助读取前的链接输入与登录提示。
- 实时任务进度。
- 可取消、可重试、连接失效和权限不足状态。
- 分析结果页。
- 来源引用与置信度。
- 用户修正和确认。
- 确认后创建/关联项目。

结果页至少展示：

- 产品一句话定义。
- 产品定位。
- 目标用户。
- 核心问题。
- 解决方案。
- 关键业务逻辑。
- 未确认问题。
- 引用来源。

### Phase 2：建立前端工程骨架

目标：在不改变视觉的前提下让代码可长期维护。

任务：

- 引入页面路由。
- 将 `main.tsx` 拆为 pages/components/features。
- 建立 `PrototypeAnalysis` 功能模块。
- 建立共享布局组件。
- 建立领域类型。
- 建立 API client 和 mock adapter。
- 建立错误边界。
- 建立基础测试。
- 建立 token，但逐项迁移，不一次性重写 CSS。

建议目录：

```text
src/
├── app/
│   ├── App.tsx
│   ├── router.tsx
│   └── providers.tsx
├── pages/
│   ├── HomePage/
│   └── ProjectBoardPage/
├── features/
│   ├── prototype-analysis/
│   ├── project-chat/
│   ├── project-tree/
│   └── inspector/
├── components/
│   ├── layout/
│   └── ui/
├── services/
├── types/
├── assets/
└── styles/
```

### Phase 3：实现真实产品资料读取

目标：让 AI 能真正得到结构化产品理解。

任务：

- 本地 Markdown/文本读取。
- Obsidian Vault 解析。
- 飞书连接器。
- Notion 连接器。
- 文档索引和相关性筛选。
- 文本去重与分块。
- 产品信息提取模型。
- 来源引用。
- 冲突检测。
- 用户确认后持久化。

### Phase 4：实现蓝湖/墨刀最小化 Computer Use Skill

目标：在没有开放读取能力时可靠提取产品文本。

任务：

- 精确链接输入。
- 登录和授权交接。
- 文档目录识别。
- 纯文本优先提取。
- 页面访问预算和 token 预算。
- 断点续读。
- 跳过 UI/代码/图片的明确策略。
- 读取范围报告。
- 失败恢复和用户接管。

### Phase 5：项目创建和持久化

目标：分析结果能够成为项目上下文，而不是一次性聊天结果。

任务：

- 用户、团队、工作区。
- 项目 CRUD。
- 项目成员和权限。
- 产品理解版本。
- 页面和画板文档。
- 评论和活动记录。
- 文件/资产存储。

### Phase 6：AI 项目画板

目标：实现可控、可追溯的 AI 设计协作。

任务：

- 消息和任务数据模型。
- 流式 AI 输出。
- 主线/分支创建。
- 分支比较与合并。
- 画布节点和页面预览。
- AI 操作日志。
- 生成、修改、撤销、重试。
- 引用产品理解、规范、Skill 和资产。

### Phase 7：设计规范、技能库和资产库

目标：让团队积累形成复利。

任务：

- 设计 token 管理。
- 组件与变体。
- 品牌资产。
- 页面模板。
- Skill 创建、版本和权限。
- 项目中调用和使用记录。
- 资产质量评分和废弃策略。

### Phase 8：协作与交付

目标：连接设计、产品和开发交付。

任务：

- 多人实时状态。
- 评论和提及。
- 审核与批准。
- 项目版本与快照。
- 代码导出。
- 资产导出。
- 交付说明。
- GitHub 集成和 PR 流程。

---

## 15. 推荐的后端与系统边界

以下是建议，不是已实现事实。接管 AI 不应在未得到用户确认前一次性搭建全部系统。

### 15.1 建议服务边界

- Web App：React 前端。
- API：用户、团队、项目、文档、资产、评论。
- AI Orchestrator：模型调用、工具规划、流式事件。
- Connector Layer：飞书、Notion、本地文件、未来其他来源。
- Browser Worker：受限平台 Computer Use 任务。
- Document Pipeline：解析、切块、去重、索引、引用。
- Storage：数据库 + 对象存储。
- Realtime：任务进度、多人状态、评论通知。

### 15.2 原型分析任务事件

前端不应该自己用计时器猜测进度。后端应发出结构化事件：

```ts
type AnalysisEvent =
  | { type: 'task.created'; taskId: string }
  | { type: 'authorization.required'; provider: string; url?: string }
  | { type: 'source.connected'; provider: string }
  | { type: 'documents.discovered'; count: number }
  | { type: 'document.reading'; title: string; index: number; total: number }
  | { type: 'content.filtering' }
  | { type: 'understanding.generating' }
  | { type: 'review.required'; resultId: string }
  | { type: 'task.completed'; resultId: string }
  | { type: 'task.failed'; code: string; message: string; retryable: boolean };
```

### 15.3 安全与隐私

- 只读取用户明确选择的范围。
- 授权最小化。
- 清楚展示读取范围。
- 支持撤销连接与删除导入数据。
- 不把用户文档用于训练，除非用户明确同意。
- 敏感文档要有团队权限隔离。
- 日志不得记录完整文档正文、Token 或密码。
- Computer Use 遇到登录、验证码和高风险权限必须让用户接管。

---

## 16. 功能完成定义（Definition of Done）

任何功能只有同时满足以下条件才算完成：

### 产品

- 对应明确的用户问题。
- 流程、空状态、加载、成功和失败状态完整。
- 文案符合中文产品语境。
- 不把技术实现负担转嫁给用户。

### 视觉

- 与指定 Paper 画板或用户标注一致。
- 常用视口下无明显溢出和错位。
- hover、focus、selected、disabled 状态完整。
- 图标风格、尺寸、位置一致。
- 未修改区域无回归。

### 工程

- TypeScript 类型清晰。
- 构建通过。
- 没有新增控制台错误。
- 关键逻辑有测试或明确人工验收步骤。
- 失败可恢复。
- 不包含密钥或真实用户数据。

### 交付

- 变更范围清楚。
- 已知限制清楚。
- 文档同步更新。
- 只有得到用户授权才提交或推送。

---

## 17. 下一位 AI 首次接管建议任务

如果用户没有给出新的具体标注，不要自主大改。建议首次接管只做：

1. 验证正确目录和 Git 状态。
2. 启动现有预览。
3. 浏览首页、原型分析和项目画板。
4. 阅读本文档并复述当前状态、已知边界和下一步建议。
5. 等待用户给出下一项明确要求。

如果用户要求继续原型分析，优先级建议为：

1. 补全分析结果确认页的前端原型。
2. 补全真实任务所需的授权、错误、取消、重试状态。
3. 再设计本地资料选择与在线连接的真实接口。
4. 最后接入真实 MCP/API/Computer Use。

---

## 18. 可直接复制给新 AI 的接管 Prompt

```text
你将继续开发 Imagine Lab（AI 设计协作系统）。

项目绝对路径：
/Users/sevenjin/Desktop/imagine lab

GitHub：
https://github.com/thereyjin/imagine-lab

开始任何工作前：
1. 执行 pwd，确认当前目录严格等于上述绝对路径。
2. 执行 git status -sb 和 git log -3 --oneline，保留所有已有修改。
3. 完整阅读根目录 AI_HANDOFF.md。
4. 读取 package.json、src/main.tsx、src/styles.css、src/precision.css、src/final-tuning.css。
5. 检查 5173 端口是否被其他项目占用；运行 npm run dev -- --host 127.0.0.1，并打开终端实际返回的 URL。
6. 如果页面出现“故事工作室、语音生成、Ethan、时间轴”等内容，立即停止，因为那不是 Imagine Lab。

必须长期遵守：
- 不重做已完成内容。
- 用户通过浏览器标注逐项调整时，只修改标注区域。
- 严格保持当前克制、专业、暖白、低对比的 UI 风格。
- 常用字体为 14px，字重主要 400/500，图标为统一线性风格。
- 文档产品使用官方真实 Logo。
- 首页快捷卡片保留暖白插图、前端文字和 Alpha Mask 淡出。
- 原型分析按读取方式分类，不按“设计原型/项目文档”分类。
- 原型分析只提炼产品是什么、服务谁、解决什么问题、如何解决及业务逻辑；跳过 UI、交互和代码。
- 飞书/Notion 优先连接器、MCP 或官方 API；蓝湖/墨刀只有没有直接读取方案时才使用 Computer Use。
- Computer Use 只读取相关产品文本，以最少页面访问和最少 token 完成任务。
- 当前 AI 分析步骤只是前端模拟，不得宣称已接入真实能力。
- 修改完成后必须运行 npm run build，并验证关键交互和指定页面。
- 除非我明确要求，不要 commit、push、删除文件、升级依赖或大规模重构。

接管后先告诉我：
1. 你确认的当前产品状态；
2. 已完成和未完成的边界；
3. 你将如何保证后续不跑偏；
然后等待我的下一项具体要求。
```

---

## 19. 最后的产品判断标准

每做一个功能，都用下面五个问题检查是否仍在接近愿景：

1. 它是否让 AI 更准确地理解产品，而不是只生成更漂亮的表面？
2. 它是否减少团队从需求到设计再到交付之间的信息损耗？
3. 它是否让团队已有规范、技能和资产更容易被复用？
4. 它是否让 AI 的过程更可控、可见、可回溯？
5. 它是否保持了 Imagine Lab 克制、专业、轻量、高品质的体验？

如果一个功能无法回答这些问题，它很可能不应该成为当前优先级。
