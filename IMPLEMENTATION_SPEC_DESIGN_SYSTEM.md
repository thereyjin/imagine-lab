# Imagine Lab：设计规范、技能、组件与页面体系重构说明

> 文档用途：交给后续 AI 编码工具直接读取并执行。
>
> 当前阶段：前端高保真原型，React + TypeScript + Vite。
>
> 核心原则：保留当前已经确认的视觉风格、无限画布交互、评论系统和页面结构。只重构本文件规定的产品逻辑及对应页面，不擅自修改其他区域。

---

## 1. 项目位置与当前代码真源

- 项目根目录：`/Users/sevenjin/Desktop/imagine lab`
- 当前实际入口：`src/main.tsx`
- 当前主要视觉样式：
  - `src/styles.css`
  - `src/precision.css`
  - `src/final-tuning.css`
- `src/app/App.tsx`、`src/app/store.ts`、`src/app/types.ts` 是另一套早期实现，目前不是浏览器中正在使用的主界面。除非明确迁移架构，否则不要把新功能实现到这套旧入口中。
- 本地预览：`http://127.0.0.1:5173/`
- 每轮修改后执行：`npm run build`

## 2. 绝对不可破坏的既有体验

1. 保留首页、统一侧边栏、项目画板、顶部导航和右侧三页签布局。
2. 保留无限画布的缩放、平移、拖动画板、点阵背景和鼠标附近点阵加深效果。
3. 保留画板顶部的“修改 / 沉淀 / 更多”工具栏，允许调整其菜单内容。
4. 保留右侧“配置 / 页面 / 评论”页签。
5. 保留画板评论定位、回复、解决、重新打开、引用到 AI 对话等已经完成的逻辑。
6. 保留当前克制、专业、暖白、低对比、细线性图标的 UI 风格。
7. 产品界面统一中文，正文和导航常用 14px；最小字号不得小于 12px。
8. 不要为了此次重构改动首页卡片、团队分享、侧边栏品牌卡或原型分析弹窗。

---

## 3. 本次重构要解决的根本问题

当前原型错误地让每张 AI 生成页面都可以“提取设计规范”，同时右侧又显示“设计规范已识别若干条”。这会造成以下误解：

- 每生成一张页面，就产生一套新的设计规范。
- 项目核心设计规范与页面总结出的规范互相冲突。
- “已识别”被误认为“已经沉淀并生效”。
- 页面、规范、组件、技能和技术规范没有明确层级。
- 顶部“沉淀”和画板旁“设计资产”重复且无法形成闭环。
- 右侧配置栏的“已生成资产”把页面和组件混在一起。

本次重构必须建立一个明确的产品模型：

> 设计规范决定页面长什么样；技能决定 AI 如何生成、检查和修改；技术规范决定代码如何实现；组件是经过验证的可复用实现；页面只是它们的组合结果。

新页面不能拥有一套新的设计规范。新页面只能：

- 引用项目核心设计规范及其版本；
- 使用项目启用的技能；
- 遵循项目技术规范；
- 调用已有组件；
- 产生新组件候选或已有组件的新变体；
- 产生规范偏差报告；
- 必要时提出“规范扩展建议”，但不能自动修改核心规范。

---

## 4. 五类核心对象及唯一职责

### 4.1 设计规范 Design System

- 层级：团队级或项目级。
- 内容：品牌原则、颜色、字体、间距、圆角、描边、阴影、栅格、图标、动效、交互状态、无障碍、响应式等。
- 机器真源：结构化 `DESIGN.md`。
- 人类视图：图示化 Sketchbook / Theme 画板。
- 设计规范不是由每个页面分别生成的。

### 4.2 技能 Skill

- 层级：团队级能力，可在项目中启用。
- 作用：告诉 AI 如何执行某类工作，例如页面生成、规范检查、组件识别、产品理解、交付检查。
- 技能不是页面资产，也不能由页面随意生成。
- 页面生成时记录调用了哪些技能以及技能版本。

### 4.3 技术规范 Tech Profile

- 层级：项目级。
- 内容：目标平台、前端框架、语言、组件框架、CSS 策略、响应式策略、目录规则、浏览器支持、测试和交付约束。
- 一旦项目确认，不应每生成一个页面重新选择。
- AI 可以推荐，但必须由用户确认。

### 4.4 组件 Component

- 层级：项目级资产，可发布到团队资产库。
- 内容：预览、代码、属性、状态、变体、使用的 Token、适用场景、来源页面、使用页面、版本。
- 新页面允许产生组件候选。
- 组件是本产品最重要的持续增长资产。

### 4.5 页面 Page

- 层级：项目级结果。
- 页面记录：名称、路由、状态、版本、使用的设计规范版本、调用技能、技术规范版本、使用组件、新组件候选、规范偏差、评论和生成记录。
- 页面信息必须进入右侧“页面”页签，不进入配置栏的“已生成资产”。

---

## 5. 项目创建模式

不要简单地把所有空白项目都视为新建设计体系。应增加 `designSystemMode`：

```ts
type DesignSystemMode = 'inherit' | 'create';
```

### 5.1 空白项目

点击“创建空白项目”完成名称输入后，进入项目初始化流程，要求用户选择：

1. 使用团队已有设计规范；
2. 从其他项目继承；
3. 创建新的设计规范。

前两项对应 `inherit`，第三项对应 `create`。

即使是空白项目，只要选择已有规范，也不能在每张页面上重新提取规范。

### 5.2 从模板创建

模板项目默认使用 `inherit`，模板必须携带：

- 绑定的设计规范 ID 和版本；
- 默认技能组合；
- 技术规范；
- 依赖的组件库；
- 页面结构和可编辑变量。

模板项目默认不提供“创建新设计规范”。用户可以替换所绑定的规范，但必须明确操作，不允许 AI 静默创建另一套规范。

### 5.3 自建设计体系

只有 `designSystemMode === 'create'` 时，项目才提供“建立核心设计规范”。该操作是项目级的一次性初始化流程，不是页面级功能。

建立后，后续页面只能引用这套规范；如发现缺失规则，只能提交规范扩展建议。

---

## 6. 推荐的数据模型

当前原型可以先用本地 React state 模拟，但字段命名和关系必须按以下模型设计，以便未来接入后端。

```ts
type ProjectMode = 'blank' | 'template' | 'imported';

interface ProjectConfig {
  id: string;
  name: string;
  mode: ProjectMode;
  designSystemMode: 'inherit' | 'create';
  designSystem: {
    id: string | null;
    name: string;
    version: string;
    scope: 'project' | 'team';
    locked: boolean;
  };
  skills: Array<{
    id: string;
    name: string;
    version: string;
    enabled: boolean;
  }>;
  techProfile: {
    id: string;
    name: string;
    version: string;
    source: 'team-default' | 'ai-recommended' | 'manual';
    confirmed: boolean;
    platform: string;
    framework: string;
    language: string;
    uiLibrary?: string;
    styling?: string;
  };
  componentLibraries: Array<{
    id: string;
    name: string;
    scope: 'project' | 'team';
  }>;
}

interface PageAsset {
  id: string;
  name: string;
  route?: string;
  status: 'draft' | 'review' | 'confirmed';
  designSystemVersion: string;
  skillVersions: string[];
  techProfileVersion: string;
  usedComponentIds: string[];
  candidateComponentIds: string[];
  deviationIds: string[];
  version: string;
}

interface ComponentCandidate {
  id: string;
  name: string;
  sourcePageId: string;
  classification:
    | 'existing-instance'
    | 'existing-variant'
    | 'new-component'
    | 'page-local';
  similarComponentId?: string;
  confidence: number;
  usagePotential: 'low' | 'medium' | 'high';
  status: 'candidate' | 'confirmed' | 'rejected' | 'published';
}

interface DesignDeviation {
  id: string;
  pageId: string;
  ruleId: string;
  type: 'violation' | 'exception' | 'extension-proposal';
  description: string;
  status: 'open' | 'accepted' | 'fixed' | 'rejected';
}
```

---

## 7. 新的页面生成链路

页面生成的输入必须在产品结构中可追溯：

```text
项目背景
＋核心设计规范及版本
＋已启用技能及版本
＋技术规范及版本
＋可用组件库
＋用户本次需求
        ↓
     AI 生成页面
        ↓
页面结果
＋已有组件调用记录
＋新组件/新变体候选
＋规范符合度与偏差
＋生成记录
```

页面生成完成后严禁展示“已生成一套设计规范”。

---

## 8. 画板顶部工具栏改造

当前代码位置：`src/main.tsx` 中 `design-toolbar` 和 `design-menu`。

### 8.1 修改

保留：

- 用 AI 修改；
- 添加评论；
- 重新生成方案。

已有批注和评论交互继续沿用，不要重做。

### 8.2 沉淀

删除当前固定入口：

- `提取设计规范`

替换为根据项目模式显示的菜单。

#### 继承设计体系的项目

- 识别可复用组件；
- 沉淀为页面模板；
- 提交规范扩展建议。

#### 尚未建立规范的自建体系项目

- 建立核心设计规范；
- 识别可复用组件；
- 沉淀为页面模板。

#### 已经建立规范的自建体系项目

- 识别可复用组件；
- 沉淀为页面模板；
- 提交规范扩展建议。

“提交规范扩展建议”必须进入审核状态，用户确认后才能生成新的 `DESIGN.md` 版本。

### 8.3 更多

保留对象管理操作：

- 页面信息；
- 版本记录；
- 复制设计稿；
- 导出页面；
- 删除设计稿。

不要把“同步亿方云”放在这里。只有已经确认并保存的资产才能在资产详情中同步。

---

## 9. 画板旁“设计资产”面板改造

当前代码位置：`src/main.tsx` 中 `design-side-panel`。

把标题“设计资产”改为“页面资产”，因为它描述的是当前页面结果。

删除：

- “设计规范 / 已引用 8 条 · 2 条待确认”作为可重新提取规范的入口。

替换为以下三部分：

### 9.1 规范符合度

示例：

- 标题：`规范符合度`
- 主信息：`使用 Imagine Design v1.3`
- 次信息：`32 项通过 · 2 项存在偏差`
- 点击后打开“规范检查详情”，而不是创建新规范。

规范检查详情包括：

- 通过项；
- 偏差项；
- 页面级例外；
- 规范扩展建议；
- 每项在画布中的定位。

### 9.2 组件使用

示例：

- 标题：`组件使用`
- 主信息：`复用 8 个已有组件`
- 次信息：`2 个新变体 · 1 个新组件候选`
- 点击后打开组件识别工作台。

### 9.3 保存状态

示例：

- 页面已自动保存；
- 组件候选尚未确认；
- 当前存储位置：本地工作区。

---

## 10. 右侧“配置”页签改造

当前代码位置：`src/main.tsx` 中 `ConfigPanel`。

配置页签按以下顺序展示：

1. 项目背景；
2. 核心设计规范；
3. 项目技能；
4. 组件来源；
5. 技术规范；
6. 组件资产。

### 10.1 核心设计规范

显示：

- 规范名称；
- 版本；
- 来源：团队 / 其他项目 / 当前项目自建；
- 状态：已锁定 / 可编辑；
- 查看 Sketchbook；
- 查看 `DESIGN.md`；
- 替换规范。

继承模式下默认锁定。需要修改时只能“提出扩展建议”或明确切换规范。

### 10.2 项目技能

显示已启用技能、版本和用途，例如：

- 页面生成 Skill；
- 设计一致性检查 Skill；
- 组件识别与去重 Skill；
- 前端交付检查 Skill。

提供“管理技能”入口。技能是项目级配置，不是页面生成资产。

### 10.3 组件来源

显示项目可调用的组件库：

- 当前项目组件库；
- 团队组件库；
- 模板依赖组件库。

用户可控制 AI 生成时允许调用哪些组件库。

### 10.4 技术规范

技术规范支持三种来源：

- 使用团队默认；
- AI 推荐；
- 手动配置。

AI 推荐前收集或推断：目标平台、现有仓库、框架、组件库、部署环境、响应式需求、团队维护能力。

推荐结果必须解释原因并要求用户确认。例如：

> 推荐 React + TypeScript + Vite，继续使用团队现有技术栈，并可直接复用当前 Ant Design 组件资产。

确认后显示规范名称、版本和关键字段。不要只显示两个无法操作的灰色文本行。

### 10.5 组件资产

把当前“已生成资产”改名为“组件资产”。

删除：

- 页面 23。

替换为：

- 已复用组件；
- 新组件候选；
- 新增组件变体；
- 待确认组件；
- 已沉淀组件。

点击每一行进入组件工作台对应筛选状态。

---

## 11. 右侧“页面”页签补全

当前页面页签只有占位文案“页面内容”，必须实现真实页面信息。

内容包括：

- 页面树 / 页面列表；
- 当前页面名称；
- 路由或业务位置；
- 页面状态：草稿、待评审、已确认；
- 页面版本；
- 使用的设计规范版本；
- 调用的技能；
- 使用的组件；
- 生成时间和生成来源；
- 前端实现状态；
- 页面级例外；
- 新建页面入口。

页面数量、页面结构和页面状态都应放在这里，不应出现在配置页签的“组件资产”中。

---

## 12. 组件识别与沉淀工作台

点击“识别可复用组件”或“组件使用”后，不要只展示三个简单候选卡片。应打开一个完整工作台。

每个识别结果必须先分类：

1. 已有组件实例：直接绑定已有组件；
2. 已有组件新变体：添加到原组件变体；
3. 新组件候选：等待命名、审核和保存；
4. 页面局部结构：不沉淀。

工作台布局建议：

- 左侧：页面截图/代码画板，候选区域高亮并可定位；
- 中间：候选列表与相似组件比较；
- 右侧：候选详情与处理动作。

候选详情包括：

- 建议名称；
- 组件预览；
- 来源页面；
- AI 判断依据；
- 相似组件；
- 复用潜力；
- 属性和状态；
- 使用的设计 Token；
- 对应代码；
- 保存范围：仅当前项目 / 发布到团队资产库。

核心操作：

- 绑定已有组件；
- 合并为已有组件变体；
- 保存为新组件；
- 标记为页面局部，不再提示。

---

## 13. 核心设计规范工作台

只有自建设计体系的项目在首次建立规范时进入该工作台；继承项目只能查看规范或提交扩展建议。

### 13.1 同一数据的两种视图

- `Theme / Sketchbook`：面向人的图示化规范画板；
- `DESIGN.md`：面向 AI 与代码生成器的结构化真源。

两者必须来自同一份状态。不能分别维护两个互不关联的数据对象。

### 13.2 Theme / Sketchbook 最低内容

- 品牌关键词和视觉原则；
- Primary / Secondary / Tertiary / Neutral 色板；
- 语义色和状态色；
- 字体族和字体层级；
- 字号、字重、行高、字间距；
- 间距单位和布局网格；
- 圆角、描边、阴影和层级；
- 按钮、输入框、卡片、导航等基础控件；
- Hover / Active / Focus / Disabled / Error / Loading 状态；
- 图标规则；
- 响应式和断点；
- 动效原则；
- 无障碍与对比度；
- 文案语气；
- 已发现冲突和待确认规则。

### 13.3 DESIGN.md 最低结构

参考用户提供的 Stitch `DESIGN.md`，但 Imagine Lab 版本必须进一步包含：

- metadata：名称、ID、版本、作用域、创建来源、更新时间；
- tokens：colors、typography、spacing、rounded、borders、shadows、motion、breakpoints；
- brandAndStyle；
- layoutAndSpacing；
- elevationAndDepth；
- iconography；
- interactionStates；
- responsiveRules；
- accessibility；
- contentTone；
- componentPrinciples；
- exceptions；
- changeLog。

每个页面只记录自己使用的 `designSystemId + version`，不复制一份新的 `DESIGN.md`。

### 13.4 保存行为

- `保存`：保存规范草稿，不影响已生成页面；
- `保存并应用`：创建新版本，并显示影响范围；
- 继承项目没有直接“保存并应用”，只能提交扩展建议；
- 设计规范升级后，需要展示受影响组件和页面。

---

## 14. 规范扩展建议

当 AI 发现现有规范无法覆盖新场景时，不创建新规范，而是生成扩展建议。

示例：

> 当前规范没有定义数据密集型表格的行高与状态色，建议新增相关规则。

扩展建议必须包含：

- 触发页面和具体位置；
- 当前缺失内容；
- 建议规则；
- 影响的组件和页面；
- 是否为页面级例外；
- 审核状态。

用户可以：

- 接受并生成设计规范新版本；
- 仅作为当前页面例外；
- 拒绝。

---

## 15. 侧边栏四个模块如何贯通

### 项目

- 页面、评论、版本、项目背景和当前生产过程。

### 资产库

- 已审核的组件；
- 页面模板；
- 品牌资产；
- 交付资产。

### 设计规范

- 团队级和项目级设计系统；
- Sketchbook；
- `DESIGN.md`；
- 版本；
- 扩展建议和影响范围。

### 技能库

- 页面生成；
- 规范检查；
- 组件识别；
- 产品理解；
- 前端交付检查等可执行方法。

正确闭环：

```text
团队设计规范 + 团队技能 + 团队组件
                ↓ 项目绑定
项目配置 + 技术规范 + 用户需求
                ↓
             生成页面
                ↓
组件候选 + 规范偏差 + 页面版本
                ↓ 人工审核
项目组件库 / 团队资产库 / 规范新版本
                ↓
           后续页面继续复用
```

---

## 16. AI 聊天框调用已有组件

在底部聊天输入框的“添加文件”附近增加“调用组件”入口，保持与现有按钮相同的视觉规范。

点击后打开组件选择器，支持：

- 搜索；
- 最近使用；
- 当前项目组件；
- 团队组件；
- 按页面类型/业务场景筛选；
- 多选；
- 查看预览和变体。

选中后，以可移除的引用标签显示在聊天输入框上方。提交需求时将组件 ID、版本和选中变体一起传入生成上下文。

不要把组件代码或大段说明直接塞进聊天框；界面只展示名称、预览缩略图和版本。

---

## 17. 需要删除或替换的现有文案与逻辑

在 `src/main.tsx` 中重点查找并处理：

1. `提取设计规范`：从普通页面的沉淀菜单删除，改为条件化入口。
2. `已引用 8 条 · 2 条待确认`：不能暗示页面产生了新规范，替换为规范符合度信息。
3. `设计资产`：改为 `页面资产`。
4. `已生成资产`：改为 `组件资产`。
5. `页面 23`：从配置页签删除，移动到页面页签。
6. `组件 58`：拆成复用、候选、变体、待确认、已沉淀状态。
7. 当前 `specs` 流程中“审核 AI 从当前页面识别出的候选规则”：普通继承项目不再使用；改造成“规范检查详情”或“规范扩展建议”。
8. 首页工作记录中的“生成新的Design规范”等演示文案，在后续统一内容清理时改为“扩展项目规范”或“沉淀组件”，避免继续传播错误心智。

---

## 18. 推荐实施顺序

### 阶段 1：先修正信息架构和假数据

- 增加项目模式和设计体系模式；
- 改造右侧配置栏；
- 补全页面页签；
- 改造画板旁页面资产面板；
- 条件化“沉淀”菜单；
- 删除错误文案。

该阶段先用前端模拟数据，不需要接后端。

### 阶段 2：实现组件工作台

- 组件候选分类；
- 相似组件比较；
- 保存、合并变体、忽略；
- 项目组件与团队组件范围；
- 画布定位；
- 聊天框调用组件。

### 阶段 3：实现设计规范工作台

- Theme / Sketchbook；
- DESIGN.md；
- 同源双视图；
- 版本；
- 保存 / 保存并应用；
- 影响范围；
- 扩展建议。

### 阶段 4：实现技能和技术规范配置

- 项目技能管理；
- 技能版本和调用记录；
- 技术规范团队默认 / AI 推荐 / 手动配置；
- 推荐说明与确认；
- 页面生成上下文可追溯。

### 阶段 5：补全侧边栏管理页面

- 资产库；
- 设计规范库；
- 技能库；
- 项目内对象与团队级对象之间的发布关系。

---

## 19. 第一轮前端实现的明确范围

如果额度或时间有限，优先完成以下可见原型，不要一次重构全部代码：

1. 右侧配置栏改为六个正确分区；
2. 页面页签展示真实页面信息；
3. 画板旁“设计资产”改为“页面资产”；
4. “设计规范”改为“规范符合度”；
5. “可复用组件”改为组件复用与候选统计；
6. 继承模式下删除“提取设计规范”，增加“提交规范扩展建议”；
7. 点击组件统计进入完整的组件候选工作台；
8. 聊天框增加“调用组件”入口；
9. 空白项目创建后增加设计体系选择；
10. 技术规范增加 AI 推荐流程。

Sketchbook / DESIGN.md 的完整编辑器可以在第二轮制作，但第一轮必须先把入口、层级和状态做正确。

---

## 20. 验收标准

完成后必须满足：

- [ ] 普通页面上不再出现“提取新设计规范”的错误心智。
- [ ] 新页面明确显示自己使用的核心设计规范版本。
- [ ] 新页面只产生组件候选、组件变体和规范偏差。
- [ ] 模板项目默认继承模板绑定的规范、技能、组件和技术规范。
- [ ] 空白项目可以选择继承已有规范或创建新规范。
- [ ] 只有自建体系项目可以建立新的核心设计规范。
- [ ] 页面数量和页面信息全部位于“页面”页签。
- [ ] 配置栏底部聚焦组件资产，而不是混合页面与组件。
- [ ] 组件候选能区分已有实例、已有变体、新组件和页面局部结构。
- [ ] AI 聊天框可以引用已有组件。
- [ ] 技术规范支持团队默认、AI 推荐和手动配置。
- [ ] 设计规范、技能、技术规范、组件和页面之间有明确版本引用关系。
- [ ] 不破坏无限画布、评论、拖拽、缩放和平移交互。
- [ ] 所有新增中文 UI 字号不小于 12px。
- [ ] `npm run build` 通过。

---

## 21. 后续 AI 执行纪律

1. 开始前读取本文件、`AI_HANDOFF.md`、当前 `src/main.tsx` 和相关 CSS。
2. 不要重做已经完成的 UI。
3. 不要把旧的 `src/app/App.tsx` 当成当前页面入口。
4. 每次只实施一个完整闭环，并在浏览器中验证。
5. 用户通过浏览器标注要求修改时，只改指定区域。
6. 不要用临时假功能掩盖错误的产品层级。
7. 演示数据可以是静态的，但状态变化必须符合本文件定义的关系。
8. 所有新增弹窗支持点击遮罩关闭；涉及未保存输入时需提供防误关保护。
9. 任何“发布到团队”“同步外部存储”“更新核心规范”的动作都必须经过用户确认。
10. 完成后执行构建，并明确报告修改范围和未完成项。

---

## 22. 一句话产品北极星

> Imagine Lab 不是让 AI 为每张页面重新发明规则，而是让团队依靠稳定的设计规范、技能、技术规范和不断成长的组件库，以越来越低的成本持续生成一致、可复用、可交付的产品页面。

